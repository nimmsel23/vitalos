# mobile: safe-area + touch defaults + mobile-shell wrapper

Adds global safe-area CSS and mobile touch defaults so UI respects device notches and insets; adds app-shell/mobile-shell classes for consistent padding; includes MOBILE_CHECKLIST for testing. Styling/docs-only changes except for classname additions.
